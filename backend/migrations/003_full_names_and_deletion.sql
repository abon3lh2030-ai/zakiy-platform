-- ============================================================================
-- إضافة عمود full_name (اسم حقيقي) لجدول profiles - يخدم "مدير المدرسة يشوف
-- اسم الطالب الكامل" (لحد الحين اسم المستخدم المولّد بس هو المخزّن، مثل
-- "sarhahmd" بدل "سارة أحمد"). حذف مدرسة (مو بس إيقافها) يعتمد على cascade
-- الموجود أصلًا بـ profiles.school_id/classes.school_id من الهجرة الأولى -
-- ما يحتاج تعديل هيكلي إضافي غير هذا العمود.
-- ============================================================================

alter table profiles add column if not exists full_name text;
